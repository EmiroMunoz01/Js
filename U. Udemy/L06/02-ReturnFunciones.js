//Declaracion de la función
function miFuncion(a,b){
    return a + b;
}

//Llamamos la funcion
//asignaremos el valor de una variable

let resultado = miFuncion(3, 2);
console.log(resultado); 


//en ocasiones no basta con mandar a imprimir el resultado, sino que necesitamos regresar el valor con el calculo que realiza una funcion, para ello usaremos return