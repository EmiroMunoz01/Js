//Tambien podemos asignar las funciones a una variable, y asi evitar  colocar un nombre a una funcion, ya que se conoceran como funciones de tipo expresion	o tambien funciones anonimas.

//Funcion de tipo expresion

let x = function (a,b){return a + b};

resultado = x(1,2);
console.log(resultado);