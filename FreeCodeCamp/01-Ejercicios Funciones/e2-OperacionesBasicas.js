//haremos las 4 operaciones basicas con dos numeros

function operaciones(a, b) {
    let suma = a + b;
    let resta = a - b;
    let multi = a * b;
    let div = a / b;
    return ("suma = "+ suma +", resta = "+ resta+", multi = "+ multi+", div = "+ div);
}

operaciones(2,2);
console.log(operaciones(2,2));