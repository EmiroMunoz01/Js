// Agrega los números de 5 a 0 (inclusivo) en orden 
// descendente a myArray usando un bucle while.

const myArray = [];

// Cambia solo el código debajo de esta línea

let i = 5;

while (i >= 0) {
    myArray.push(i);
    i--;
}

console.log(myArray);