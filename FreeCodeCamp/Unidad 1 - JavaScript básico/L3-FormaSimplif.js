function isEqual(a, b) {
    if (a === b) {
      return true;
    } 
    else {
      return false;
    }
}

//ambas expresiones son exactamente lo mismo, pero abajo la tenemos de forma simplificada

function isEqual(a, b) {
    return a === b;
}